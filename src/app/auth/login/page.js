import React from 'react';
import Login from './Login';

export const metadata = {
    title: "Login | Task-Manager"
}

const page = () => {
    return (
        <div>
            <Login />
        </div>
    );
};

export default page;